{
  "name": "server-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provider-neutral Context OS external entry-point routing contract.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
